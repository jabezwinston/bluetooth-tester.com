# ESP32-classic only: older ESP32 silicon (rev 0/1, common on DevKitC/WROOM) is gated off in recent
# Zephyr — soc_init errors out and the app never starts (so HCI never comes up). Opt in to run on it.
CONFIG_ESP32_USE_UNSUPPORTED_REVISION=y
